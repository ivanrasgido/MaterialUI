import { Grid } from '@material-ui/core';
import React from 'react';

export default function GridLayout(){
    return(
        <Grid container>
            <Grid item>

            </Grid>
            <Grid item>

            </Grid>
        </Grid>
    );
}